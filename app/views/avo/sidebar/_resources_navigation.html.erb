<%= sidebar_link t('avo.resources'), nil, large: true %>

<div class="w-full">
  <% resources.each do |resource| %>
    <% # @todo: maybe cache this partial ? %>
    <%= sidebar_link resource.name.pluralize.humanize, resources_path(resource.model_class) %>
  <% end %>
</div>
