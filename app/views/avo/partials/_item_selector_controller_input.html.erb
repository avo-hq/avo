<input type="checkbox"
  class="mx-3 <%= 'absolute inset-auto left-0 mt-2 z-10 hidden group-hover:block checked:block' if local_assigns[:floating] %> <%= local_assigns[:size].present? && local_assigns[:size].to_sym == :lg ? 'w-5 h-5' : 'w-4 h-4' %>"
  data-action="input->item-selector#toggle"
/>
