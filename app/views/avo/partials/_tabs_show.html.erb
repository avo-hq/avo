<%= turbo_frame_tag group.turbo_frame_id do %>
  <div class="border rounded-lg p-2 -mx-2 -my-2 lg:p-4 lg:-mx-4 lg:-my-4">
    <div class="mb-4">
      <%= render partial: 'avo/partials/tabs_toggle', locals: { group: group, tabs: group.items, active_tab_name: active_tab.name } %>
    </div>
    <div class="space-y-12">
      <% active_tab.items.each do |item| %>
        <%= render Avo::ItemSwitcherComponent.new resource: @resource, item: item, index: index, view: @view %>
        <%#= render partial: 'avo/partials/item_switcher', locals: { item: item, index: index, form: form, main_panel_tools: main_panel_tools } %>
      <% end %>
    </div>
  </div>
<% end %>
