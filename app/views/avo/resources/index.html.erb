<%= turbo_frame_start params[:frame_name] %>
<%= render(Avo::ResourceIndexComponent.new(
  title: @heading,
  resource: @resource,
  resources: @resources,
  models: @models,
  view_type: @view_type,
  params: params,
)) %>
<%= turbo_frame_end params[:frame_name] %>
