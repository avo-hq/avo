<% if params[:frame_name].present? %>
  <turbo-frame id="<%= params[:frame_name] %>">
<% end %>
  <%= render(Avo::ResourceIndexComponent.new(
    title: @resource.plural_name,
    resource: @resource,
    resources: @resources,
    models: @models,
    view_type: @view_type,
    via_relation: @via_relation,
  )) %>

<% if params[:frame_name].present? %>
  </turbo-frame>
<% end %>
