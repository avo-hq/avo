<div class="flex flex-row space-x-2">
  <%# add via_resource_name and via_resource_id params  %>
  <%# add authorization %>
  <%# if can view %>
  <%= link_to svg('media/svgs/eye.svg', class: 'text-gray-500 h-6 hover:text-gray-600'), resource_path(resource[:model]), title: t('avo.view_item', { item: resource[:model].model_name.name.downcase }).capitalize, data_control: 'view' %>
  <%# if can edit %>
  <%= link_to svg('media/svgs/edit.svg', class: 'text-gray-500 h-6 hover:text-gray-600'), edit_resource_path(resource[:model]), title: t('avo.edit_item', { item: resource[:model].model_name.name.downcase }).capitalize, data_control: 'edit' %>
  <%# @todo: add delete & detach actions %>
</div>
