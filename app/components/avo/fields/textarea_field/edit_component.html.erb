<%= field_wrapper **field_wrapper_args do %>
  <%= @form.text_area @field.id, **field_input_args.merge({
    class: classes("w-full"),
    placeholder: @field.placeholder,
    rows: @field.rows
  })%>
<% end %>
