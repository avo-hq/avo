<%= field_wrapper **field_wrapper_args do %>
  <div class="flex gap-1 items-center flex-wrap">
    <% @field.field_value.each do |item| %>
      <%= render Avo::Fields::TagsField::TagComponent.new(label: item[:label]) %>
    <% end %>
  </div>
<% end %>
