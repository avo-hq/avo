<%= field_wrapper **field_wrapper_args do %>
  <% if field.value.present? %>
    <%= area_map [
                   {
                     geometry: {
                       type: "Polygon", coordinates: JSON.parse(field.value)
                     }
                   }
                 ], id: "area-map", style: 'mapbox://styles/mapbox/satellite-v9' %>
  <% else %>
    —
  <% end %>
<% end %>

