<%= field_wrapper **field_wrapper_args do %>
  <%= form.text_field @field.id, field_input_args.merge({
    class: classes("w-full"),
    placeholder: @field.placeholder,
    multiple: multiple,
    autocomplete: @field.autocomplete
  })%>
<% end %>
