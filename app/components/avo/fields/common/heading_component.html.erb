<div class="flex items-start py-1 leading-tight bg-gray-150 text-gray-500 text-xs">
  <div class="p-3 px-6 h-full w-full">
    <% if @value.present? %>
      <% if @as_html %>
        <%== @value %>
      <% else %>
        <div class="font-semibold uppercase"><%= @value %></div>
      <% end %>
    <% else %>
      —
    <% end %>
  </div>
</div>
