<%= content_tag :div, class: "#{view_type == "grid" ? "flex-wrap" : "space-x-2 rtl:space-x-reverse"} flex flex-row justify-evenly ms-2 has-record-selector:ms-0" do %>
  <% @resource.render_row_controls(item: singular_resource_name).each do |control| %>
    <%= render_control control %>
  <% end %>
<% end %>
