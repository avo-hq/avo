<%= content_tag :div, class: class_names("dropdown-card", @classes) do %>
  <%= header %>

  <% if body? %>
    <div class="dropdown-card__body">
      <%= body %>
    </div>
  <% end %>

  <%= footer %>
<% end %>
