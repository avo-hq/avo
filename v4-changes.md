# Changes

### Components
- `Panel` -> `DetailView`
- `Panel` becomes the shell that has a title, description on gray and the main content on white

### discreet_information
- `label` is now `text`
- `url_target` is now `target`
- `as` can be `icon`, `text`, `badge`, `key_value`
- `key_value` has a `key` and `text`
- [] # TODO: support `url` and `target` on cado discreet_information component
