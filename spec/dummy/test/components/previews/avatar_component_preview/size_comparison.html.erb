<div style="padding: 2rem;">
  <div style="text-align: center; margin-bottom: 2rem;">
    <h2 style="font-size: 1.8rem; margin-bottom: 0.5rem;">📏 Size Comparison</h2>
    <p style="color: #666; margin-bottom: 1rem;">Compare avatar sizes across different configurations</p>

    <div
      style="display: inline-flex; gap: 1rem; padding: 1rem; background: #f8f9fa; border-radius: 8px; margin-bottom: 2rem;"
    >
      <span><strong>Type:</strong> <%= avatar_type.capitalize %></span>
      <span><strong>Background:</strong> <%= background_color.capitalize %></span>
      <span><strong>Labels:</strong> <%= show_labels ? "Shown" : "Hidden" %></span>
    </div>
  </div>

  <div
    style="<%= background_styles = {
      'white' => 'background: white; border: 1px solid #e1e5e9;',
      'gray' => 'background: #f8f9fa; border: 1px solid #dee2e6;',
      'dark' => 'background: #343a40; color: white; border: 1px solid #495057;'
    }
    "#{background_styles[background_color]} padding: 3rem; border-radius: 12px;" %>"
  >
    <div style="display: flex; justify-content: center; align-items: end; gap: 3rem; flex-wrap: wrap;">
      <% sizes = [
          { name: 'Large', key: 'large', pixels: '80px' },
          { name: 'Medium', key: 'medium', pixels: '48px' },
          { name: 'Small', key: 'small', pixels: '32px' },
          { name: 'Tiny', key: 'tiny', pixels: '20px' }
        ]

        avatar_configs = {
          'placeholder' => { type: 'placeholder' },
          'initial' => { type: 'initial', initial: 'A', theme: 'blue' },
          'avatar' => {
            type: 'avatar',
            src: 'https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=400&h=400&fit=crop&crop=face',
            alt: 'Sample User'
          }
        } %>

      <% sizes.each do |size_info| %>
        <div style="display: flex; flex-direction: column; align-items: center; gap: 1rem;">
          <% if show_labels %>
            <div style="text-align: center; margin-bottom: 0.5rem;">
              <div style="font-weight: 600; font-size: 1rem;"><%= size_info[:name] %></div>
              <div style="font-size: 0.8rem; opacity: 0.7;"><%= size_info[:pixels] %></div>
            </div>
          <% end %>

          <%= render Avo::AvatarComponent.new(
            **avatar_configs[avatar_type].merge(size: size_info[:key])
          ) %>
        </div>
      <% end %>
    </div>

    <% if show_labels %>
      <div
        style="text-align: center; margin-top: 2rem; padding-top: 2rem; border-top: 1px solid <%= background_color == 'dark' ? '#495057' : '#e1e5e9' %>;"
      >
        <div style="font-size: 0.9rem; opacity: 0.8;">
          Pixel dimensions: Large (80×80) • Medium (48×48) • Small (32×32) • Tiny (20×20)
        </div>
      </div>
    <% end %>
  </div>
</div>
