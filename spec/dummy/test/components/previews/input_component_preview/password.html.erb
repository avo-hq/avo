<div class="p-8 space-y-8 bg-white">
  <div class="text-lg font-semibold text-content mb-6">Password Field – States</div>

  <div class="grid grid-cols-3 gap-6">
    <!-- Header Row -->
    <div class="text-sm font-semibold text-content">Small (sm)</div>
    <div class="text-sm font-semibold text-content">Medium (md)</div>
    <div class="text-sm font-semibold text-content">Large (lg)</div>

    <!-- Error -->
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Error</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input input-wrapper--loading" data-controller="password-visibility">
            <input
              type="password"
              class="input--size-sm input-field--error"
              placeholder="Password"
              value=" dasdasdadasda dasdasdadasdadasdasdadasdadasdasdadasda"
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <span class="field-wrapper__error mt-2">Password is required</span>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Error</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              type="password"
              class="input-field--error"
              placeholder="Password"
              value=""
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__error mt-2">Password is required</div>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Error</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              type="password"
              class="input--size-lg input-field--error"
              placeholder="Password"
              value=""
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__error mt-2">Password is required</div>
        </div>
      </div>
    </div>

    <!-- Success -->
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Success</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              type="password"
              class="input--size-sm input-field--success"
              placeholder="Password"
              value=""
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Success</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              type="password"
              class="input--size-md input-field--success"
              placeholder="Password"
              value=""
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Success</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              type="password"
              class="input--size-lg input-field--success"
              placeholder="Password"
              value=""
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>


    <!-- Desabled password -->
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Disabled</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              disabled
              type="password"
              class="input--size-sm
              placeholder="Password"
              value="disabled password"
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Disabled</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              disabled
              type="password"
              class="input--size-md
              placeholder="Password"
              value="disabled password"
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Disabled</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input relative" data-controller="password-visibility">
            <input
              disabled
              type="password"
              class="input--size-lg
              placeholder="Password"
              value="disabled password"
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>

    <!-- Disabled and loading -->
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Disabled and loading</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input input-wrapper--loading" data-controller="password-visibility">
            <input
              disabled
              type="password"
              class="input--size-sm"
              placeholder="Password"
              value="disabled password disabled passworddisabled passworddisabled passworddisabled password"
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Disabled and loading</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input input-wrapper--loading" data-controller="password-visibility">
            <input
              disabled
              type="password"
              class="input--size-md"
              placeholder="Password"
              value="disabled password disabled passworddisabled passworddisabled passworddisabled password"
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>
    <div>
      <div class="text-xs font-medium text-content-secondary mb-2">Disabled and loading</div>
      <div class="field-wrapper--full-width">
        <div class="field-wrapper__content-wrapper">
          <div class="field-wrapper__input input-wrapper--loading" data-controller="password-visibility">
            <input
              disabled
              type="password"
              class="input--size-lg"
              placeholder="Password"
              value="disabled password disabled passworddisabled passworddisabled passworddisabled password"
              autocomplete="off"
              data-password-visibility-target="input"
            >
            <button type="button" class="input-field__password-toggle" data-action="password-visibility#toggle" aria-label="Show password">
              <%= svg("tabler/outline/eye", data: { "password-visibility-target": "icon" }) %>
              <%= svg("tabler/outline/eye-off", class: "hidden", data: { "password-visibility-target": "icon" }) %>
            </button>
          </div>
          <div class="field-wrapper__help">Password is required</div>
        </div>
      </div>
    </div>
  </div>
</div>
